<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= novel.title %> - อ่านนิยาย</title>
    
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <script>
        (function() {
            const savedTheme = localStorage.getItem('theme') || 'light';
            document.documentElement.setAttribute('data-theme', savedTheme);
        })();
    </script>
    
    <style>
        /* 🟢 Preload Class */
        .preload * { transition: none !important; -webkit-transition: none !important; }

        /* --- Page Specific Styles --- */
        .novel-header-wrapper {
            display: grid;
            grid-template-columns: 280px 1fr;
            gap: 40px;
            align-items: start;
        }

        /* รูปปก */
        .novel-cover-box {
            position: relative;
            border-radius: var(--radius);
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.15);
            aspect-ratio: 2/3;
        }
        .novel-cover-box img {
            width: 100%; height: 100%; object-fit: cover; transition: transform 0.3s;
        }
        .novel-cover-box:hover img { transform: scale(1.03); }

        /* ข้อมูลนิยาย */
        .novel-info-header h1 {
            font-size: 2.5rem; font-weight: 800; line-height: 1.2; margin: 0 0 10px 0;
            background: linear-gradient(135deg, var(--text-main) 0%, var(--primary) 100%);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
        }
        
        .novel-meta-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
            gap: 15px; margin: 20px 0; padding: 15px;
            background: rgba(255,255,255,0.3);
            border-radius: var(--radius-sm); border: 1px solid var(--border);
        }

        .meta-item { display: flex; flex-direction: column; gap: 4px; }
        .meta-label { font-size: 0.8rem; color: var(--text-muted); text-transform: uppercase; font-weight: 600; }
        .meta-value { font-weight: 500; color: var(--text-main); }

        /* Tags */
        .tag-container { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 25px; }
        .tag-pill {
            background: rgba(99, 102, 241, 0.08); color: var(--primary);
            padding: 6px 14px; border-radius: 50px; font-size: 0.85rem; font-weight: 600; transition: all 0.2s;
        }
        .tag-pill:hover { background: var(--primary); color: white; transform: translateY(-2px); }

        /* เรื่องย่อ */
        .synopsis-box {
            background: rgba(var(--bg-card), 0.5); padding: 24px;
            border-radius: var(--radius-sm); border: 1px solid var(--border);
            line-height: 1.8; color: var(--text-muted); position: relative;
        }

        /* ปุ่ม Action */
        .action-buttons { display: flex; flex-direction: column; gap: 12px; margin-top: 24px; }

        /* --- Chapter List Section (New UI) --- */
        .chapter-section {
            margin-top: 40px;
            background: var(--bg-card);
            border-radius: var(--radius);
            border: 1px solid var(--border);
            padding: 24px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.03);
        }

        .chapter-controls-wrapper {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 24px;
            padding-bottom: 20px;
            border-bottom: 1px solid var(--border);
        }

        .chapter-search-box {
            position: relative;
            flex: 1;
            min-width: 200px;
            max-width: 400px;
        }
        .chapter-search-box input {
            width: 100%;
            padding: 10px 15px 10px 40px;
            border-radius: 50px;
            border: 1px solid var(--border);
            background: var(--bg-input);
            color: var(--text-main);
            outline: none;
            transition: border-color 0.2s;
        }
        .chapter-search-box input:focus { border-color: var(--primary); }
        .chapter-search-box i {
            position: absolute; left: 15px; top: 50%; transform: translateY(-50%);
            color: var(--text-muted);
        }

        .filter-actions {
            display: flex; gap: 10px; align-items: center;
        }

        .chapter-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 12px;
        }

        .chapter-item {
            background: var(--bg-input);
            border: 1px solid transparent;
            border-radius: 8px;
            padding: 14px 18px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            text-decoration: none;
            color: var(--text-main);
            transition: all 0.2s;
            position: relative;
        }
        .chapter-item:hover {
            border-color: var(--primary);
            background: var(--bg-card);
            transform: translateX(5px);
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        }
        .chapter-item.read { opacity: 0.6; }
        .chapter-item.read .fa-check-circle { display: inline-block; color: var(--success); }
        .chapter-item .fa-check-circle { display: none; margin-left: 8px; }

        .chapter-info h4 { margin: 0; font-size: 0.95rem; font-weight: 500; }
        .chapter-date { font-size: 0.75rem; color: var(--text-muted); margin-top: 4px; }

        /* View Mode: List */
        .chapter-grid.view-list { grid-template-columns: 1fr; gap: 8px; }
        
        /* Toast */
        .toast-container { position: fixed; top: 90px; right: 20px; z-index: 1000; display: flex; flex-direction: column; gap: 10px; }
        .toast {
            background: var(--bg-card); color: var(--text-main); padding: 12px 20px;
            border-radius: var(--radius-sm); box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            border-left: 4px solid var(--primary); display: flex; align-items: center; gap: 10px;
            animation: slideIn 0.3s ease;
        }
        .toast.error { border-left-color: var(--danger); }
        @keyframes slideIn { from { transform: translateX(100%); opacity: 0; } to { transform: translateX(0); opacity: 1; } }

        /* Responsive */
        @media (max-width: 900px) { .novel-header-wrapper { grid-template-columns: 220px 1fr; gap: 30px; } }
        @media (max-width: 768px) {
            .novel-header-wrapper { grid-template-columns: 1fr; text-align: center; gap: 20px; }
            .novel-cover-box { width: 200px; margin: 0 auto; }
            .novel-info-header h1 { font-size: 1.8rem; }
            .novel-meta-grid { grid-template-columns: 1fr 1fr; text-align: left; }
            .tag-container { justify-content: center; }
            .action-buttons { flex-direction: row; }
            .action-buttons .btn { flex: 1; }
            .chapter-controls-wrapper { flex-direction: column; align-items: stretch; }
            .chapter-search-box { max-width: 100%; }
            .filter-actions { justify-content: space-between; }
        }
    </style>
</head>
<body class="preload">
    <div id="toast-container" class="toast-container"></div>

    <header class="main-header">
        <div class="d-flex justify-between align-center w-100" style="padding: 0 24px; max-width: 100%;">
            <nav class="breadcrumbs" style="display: flex; align-items: center; gap: 8px; font-size: 0.9rem;">
                <a href="/" style="text-decoration: none; color: var(--text-muted); transition: color 0.2s;">
                    <i class="fa-solid fa-house"></i> หน้าหลัก
                </a>
                <span style="color: var(--text-muted); opacity: 0.5;">/</span>
                <span style="font-weight: 600; color: var(--primary); max-width: 200px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">
                    <%= novel.title %>
                </span>
            </nav>
            <div class="d-flex align-center gap-2">
                 <button onclick="toggleTheme()" class="btn btn-outline btn-sm" style="border-radius:50%; width:40px; height:40px; padding:0;">
                    <i class="fa-solid fa-moon"></i>
                </button>
            </div>
        </div>
    </header>

    <div class="container" style="padding-top: 40px;">
        <div class="glass-panel novel-header-wrapper">
            <div>
                <div class="novel-cover-box">
                    <img src="<%= novel.imageUrl || defaultCover %>" onerror="this.onerror=null;this.src='<%= defaultCover %>';" alt="<%= novel.title %>">
                    <div class="novel-badge badge-<%= novel.status === 'จบแล้ว' ? 'end' : 'on' %>" style="position:absolute; top:10px; left:10px;">
                        <%= novel.status %>
                    </div>
                </div>

                <div class="action-buttons">
                    <button id="continue-reading-btn" class="btn btn-primary w-100" style="display:none;" onclick="continueReading()">
                        <i class="fa-solid fa-play"></i> อ่านต่อ
                    </button>
                    <% if(totalChapters > 0) { %>
                        <a href="/chapter/<%= chapters.length > 0 ? chapters[chapters.length-1]._id : '#' %>" class="btn btn-outline w-100">
                            <i class="fa-solid fa-book-open"></i> เริ่มอ่านตอนแรก
                        </a>
                    <% } %>
                </div>
            </div>

            <div class="novel-info-header">
                <% if(isAdmin) { %>
                    <div class="d-flex justify-between align-center mb-4" style="background: rgba(0,0,0,0.05); padding: 8px 15px; border-radius: 8px;">
                        <span class="text-muted" style="font-size:0.8rem;"><i class="fa-solid fa-user-shield"></i> เมนูแอดมิน</span>
                        <div class="d-flex gap-2">
                            <a href="/novel/<%= novel._id %>/edit" class="btn btn-sm btn-outline"><i class="fa-solid fa-pen"></i> แก้ไข</a>
                            <form action="/novel/<%= novel._id %>?_method=DELETE" method="POST" onsubmit="return confirm('⚠️ ยืนยันการลบนิยาย?');" style="margin:0;">
                                <button type="submit" class="btn btn-sm btn-outline" style="color: var(--danger); border-color: var(--danger);"><i class="fa-solid fa-trash"></i> ลบ</button>
                            </form>
                        </div>
                    </div>
                <% } %>

                <h1><%= novel.title %></h1>
                
                <% if(novel.originalTitle) { %>
                    <div class="text-muted" style="font-size: 1.1rem; margin-bottom: 20px; font-weight: 300;">
                        <%= novel.originalTitle %>
                    </div>
                <% } %>

                <% if(novel.tags && novel.tags.length > 0) { %>
                    <div class="tag-container">
                        <% novel.tags.forEach(tag => { %>
                            <a href="/search?tag=<%= tag %>" class="tag-pill">#<%= tag %></a>
                        <% }) %>
                    </div>
                <% } %>

                <div class="novel-meta-grid">
                    <div class="meta-item">
                        <span class="meta-label"><i class="fa-solid fa-feather"></i> ผู้แต่ง</span>
                        <span class="meta-value"><%= novel.author %></span>
                    </div>
                    <div class="meta-item">
                        <span class="meta-label"><i class="fa-solid fa-eye"></i> ยอดวิว</span>
                        <span class="meta-value"><%= (novel.views || 0).toLocaleString() %></span>
                    </div>
                    <div class="meta-item">
                        <span class="meta-label"><i class="fa-solid fa-layer-group"></i> จำนวนตอน</span>
                        <span class="meta-value"><%= totalChapters.toLocaleString() %> ตอน</span>
                    </div>
                    <div class="meta-item">
                        <span class="meta-label"><i class="fa-solid fa-calendar"></i> อัปเดตล่าสุด</span>
                        <span class="meta-value"><%= new Date(novel.updatedAt).toLocaleDateString('th-TH') %></span>
                    </div>
                </div>

                <% if (user) { %>
                    <button onclick="toggleFavorite()" id="fav-btn" class="btn <%= isFav ? 'btn-primary' : 'btn-outline' %> w-100" style="margin-top: 10px;">
                        <i class="fa-<%= isFav ? 'solid' : 'regular' %> fa-heart"></i> 
                        <span id="fav-text"><%= isFav ? 'ติดตามแล้ว' : 'กดติดตาม' %></span>
                    </button>
                <% } %>

                <script>
                async function toggleFavorite() {
                    try {
                        const res = await fetch(`/novel/<%= novel._id %>/favorite`, { method: 'POST', headers: {'Accept': 'application/json'} });
                        const data = await res.json();
                        const btn = document.getElementById('fav-btn');
                        const icon = btn.querySelector('i');
                        const text = document.getElementById('fav-text');
                        if (data.isFav) { // Add
                            btn.classList.remove('btn-outline'); btn.classList.add('btn-primary');
                            icon.classList.remove('fa-regular'); icon.classList.add('fa-solid');
                            text.innerText = 'ติดตามแล้ว';
                        } else { // Remove
                            btn.classList.remove('btn-primary'); btn.classList.add('btn-outline');
                            icon.classList.remove('fa-solid'); icon.classList.add('fa-regular');
                            text.innerText = 'กดติดตาม';
                        }
                    } catch(e) { console.error(e); }
                }
                </script>

                <div class="synopsis-box">
                    <h4 style="margin-top:0; color:var(--text-main); margin-bottom: 12px; font-weight: 700;">
                        <i class="fa-solid fa-align-left" style="color:var(--primary);"></i> เรื่องย่อ
                    </h4>
                    <div style="white-space: pre-line;"><%= novel.description || "ไม่มีคำอธิบาย" %></div>
                    
                    <% if(novel.originalLink) { %>
                        <div style="margin-top: 20px; border-top: 1px dashed var(--border); padding-top: 10px;">
                            <a href="<%= novel.originalLink %>" target="_blank" class="btn-text" style="color: var(--accent); text-decoration: none; font-size: 0.9rem;">
                                <i class="fa-solid fa-external-link-alt"></i> แหล่งที่มาต้นฉบับ
                            </a>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <% if (isAdmin) { %>
        <div class="card d-flex align-center justify-between" style="border: 1px dashed var(--primary); background: rgba(99, 102, 241, 0.03); margin-top: 30px;">
            <div class="d-flex align-center gap-2">
                <div style="background: var(--primary); color: white; width: 45px; height: 45px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 1.2rem;">
                    <i class="fa-solid fa-plus"></i>
                </div>
                <div>
                    <h4 style="margin: 0;">เพิ่มตอนใหม่</h4>
                    <span class="text-muted" style="font-size: 0.9rem;">แปลด้วย AI หรือลงเนื้อหาเอง</span>
                </div>
            </div>
            <a href="/novel/<%= novel._id %>/add" class="btn btn-primary">
                ไปยังหน้าเพิ่มตอน <i class="fa-solid fa-arrow-right"></i>
            </a>
        </div>
        <% } %>

        <div class="chapter-section">
            <div class="chapter-controls-wrapper">
                <div>
                    <h3 style="margin:0; font-size: 1.5rem; font-weight: 700; display:flex; align-items:center; gap:10px;">
                        <i class="fa-solid fa-list-ol" style="color:var(--primary);"></i> สารบัญ
                    </h3>
                </div>

                <form action="/novel/<%= novel._id %>" method="GET" class="chapter-search-box">
                    <input type="hidden" name="sort" value="<%= sortOrder %>"> <i class="fa-solid fa-search"></i>
                    <input type="text" name="qChapter" placeholder="ค้นหาตอน... (เช่น 1, บทนำ)" value="<%= typeof chapterSearch !== 'undefined' ? chapterSearch : '' %>">
                </form>

                <div class="filter-actions">
                    <div class="rank-tabs" style="margin:0;">
                        <button class="rank-tab active" id="btn-grid" onclick="setView('grid')"><i class="fa-solid fa-border-all"></i></button>
                        <button class="rank-tab" id="btn-list" onclick="setView('list')"><i class="fa-solid fa-list-ul"></i></button>
                    </div>
                    
                    <a href="/novel/<%= novel._id %>?sort=<%= sortOrder === 'newest' ? 'oldest' : 'newest' %>&qChapter=<%= typeof chapterSearch !== 'undefined' ? chapterSearch : '' %>" 
                    class="btn btn-outline btn-sm" 
                    title="เรียงลำดับ <%= sortOrder === 'newest' ? 'เก่า -> ใหม่' : 'ใหม่ -> เก่า' %>">
                        <i class="fa-solid fa-arrow-down-up-across-line"></i> 
                        <%= sortOrder === 'newest' ? 'ล่าสุด' : 'แรกสุด' %>
                    </a>
                </div>
            </div>

            <% if(chapters.length === 0) { %>
                <div class="text-center text-muted" style="padding: 40px;">
                    <i class="fa-regular fa-folder-open" style="font-size: 3rem; margin-bottom: 10px; opacity: 0.5;"></i>
                    <p>ไม่พบตอนที่ค้นหา</p>
                    <% if(typeof chapterSearch !== 'undefined' && chapterSearch) { %>
                        <a href="/novel/<%= novel._id %>" class="btn btn-sm btn-outline">ล้างคำค้นหา</a>
                    <% } %>
                </div>
            <% } else { %>
                <div id="chapterContainer" class="chapter-grid">
                    <% chapters.forEach(chapter => { %>
                        <div class="chapter-item-wrapper" style="position: relative;">
                            <a href="/chapter/<%= chapter._id %>" class="chapter-item" id="chapter-<%= chapter._id %>">
                                <div class="chapter-info" style="flex: 1;">
                                    <h4 class="text-truncate">
                                        Ep.<%= chapter.chapterNumber %> : <%= chapter.title.replace(/^ตอนที่ \d+[:\s]*/, '') %>
                                        <i class="fa-solid fa-check-circle"></i>
                                    </h4>
                                    <div class="chapter-date">
                                        <i class="fa-regular fa-clock"></i> <%= new Date(chapter.createdAt).toLocaleDateString('th-TH') %>
                                    </div>
                                </div>
                            </a>

                            <% if (isAdmin) { %>
                                <div style="position: absolute; right: 10px; top: 50%; transform: translateY(-50%); display: flex; gap: 5px; z-index: 2;">
                                    <a href="/chapter/<%= chapter._id %>/edit" class="btn btn-sm btn-outline" style="background:var(--bg-card); width:30px; height:30px; padding:0; display:flex; align-items:center; justify-content:center;"><i class="fa-solid fa-pen" style="font-size:0.8rem;"></i></a>
                                    <form action="/chapter/<%= chapter._id %>?_method=DELETE" method="POST" onsubmit="return confirm('ลบตอนนี้?')" style="margin:0;">
                                        <button type="submit" class="btn btn-sm btn-outline" style="color: var(--danger); background:var(--bg-card); border-color:transparent; width:30px; height:30px; padding:0;"><i class="fa-solid fa-trash" style="font-size:0.8rem;"></i></button>
                                    </form>
                                </div>
                            <% } %>
                        </div>
                    <% }) %>
                </div>
            <% } %>

            <% if (totalPages > 1) { %>
                <div class="pagination-container" style="display: flex; justify-content: center; gap: 8px; margin-top: 40px; flex-wrap: wrap;">
                    <% if (currentPage > 1) { %>
                        <a href="/novel/<%= novel._id %>?page=<%= currentPage - 1 %>&sort=<%= sortOrder %>&qChapter=<%= typeof chapterSearch !== 'undefined' ? chapterSearch : '' %>" 
                           class="btn btn-outline" style="border-radius: 8px;">
                           <i class="fa-solid fa-chevron-left"></i>
                        </a>
                    <% } %>

                    <% for(let i = 1; i <= totalPages; i++) { %>
                        <% if (i === 1 || i === totalPages || (i >= currentPage - 2 && i <= currentPage + 2)) { %>
                            <a href="/novel/<%= novel._id %>?page=<%= i %>&sort=<%= sortOrder %>&qChapter=<%= typeof chapterSearch !== 'undefined' ? chapterSearch : '' %>" 
                               class="btn <%= currentPage === i ? 'btn-primary' : 'btn-outline' %>" 
                               style="width: 40px; height: 40px; padding: 0; display: flex; align-items: center; justify-content: center; border-radius: 8px;">
                               <%= i %>
                            </a>
                        <% } else if (i === currentPage - 3 || i === currentPage + 3) { %>
                            <span class="text-muted" style="align-self: flex-end; padding: 0 5px;">...</span>
                        <% } %>
                    <% } %>

                    <% if (currentPage < totalPages) { %>
                        <a href="/novel/<%= novel._id %>?page=<%= currentPage + 1 %>&sort=<%= sortOrder %>&qChapter=<%= typeof chapterSearch !== 'undefined' ? chapterSearch : '' %>" 
                           class="btn btn-outline" style="border-radius: 8px;">
                           <i class="fa-solid fa-chevron-right"></i>
                        </a>
                    <% } %>
                </div>
            <% } %>
        </div>
    </div>

    <script>
        const novelId = "<%= novel._id %>";
        
        function getReadHistory() {
            try {
                const raw = localStorage.getItem(`readHistory-${novelId}`);
                return raw ? JSON.parse(raw) : [];
            } catch(e) { return []; }
        }

        function checkReadHistory() {
            // 1. ปุ่มอ่านต่อ
            const lastRead = localStorage.getItem(`lastRead-${novelId}`);
            if (lastRead) {
                const btn = document.getElementById('continue-reading-btn');
                if(btn) {
                    btn.style.display = 'inline-flex';
                    btn.onclick = (e) => {
                        e.preventDefault();
                        window.location.href = `/chapter/${lastRead}`;
                    };
                }
            }
            // 2. Mark as read
            const history = getReadHistory();
            history.forEach(id => {
                const chapterItem = document.getElementById(`chapter-${id}`);
                if (chapterItem) {
                    chapterItem.classList.add('read');
                }
            });
        }

        function setView(mode) {
            const container = document.getElementById('chapterContainer');
            const btnGrid = document.getElementById('btn-grid');
            const btnList = document.getElementById('btn-list');
            if(!container || !btnGrid || !btnList) return; // กัน Error ถ้าไม่พบ Element

            if(mode === 'list') {
                container.classList.add('view-list');
                btnList.classList.add('active'); 
                btnGrid.classList.remove('active');
            } else {
                container.classList.remove('view-list');
                btnGrid.classList.add('active'); 
                btnList.classList.remove('active');
            }
            localStorage.setItem('chapterView', mode);
        }

        // Theme Toggler
        function toggleTheme() {
            const current = document.documentElement.getAttribute('data-theme');
            const next = current === 'light' ? 'dark' : (current === 'dark' ? 'sepia' : 'light');
            document.documentElement.setAttribute('data-theme', next);
            localStorage.setItem('theme', next);
        }

        document.addEventListener('DOMContentLoaded', () => {
            document.documentElement.setAttribute('data-theme', localStorage.getItem('theme') || 'light');
            const savedView = localStorage.getItem('chapterView') || 'grid';
            setView(savedView);
            checkReadHistory(); 
            setTimeout(() => document.body.classList.remove('preload'), 100);
        });
    </script>
</body>
</html>