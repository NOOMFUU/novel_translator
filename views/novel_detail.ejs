<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= novel.title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* CSS เฉพาะหน้าสำหรับ List View */
        .chapter-grid.view-list {
            grid-template-columns: 1fr;
            gap: 10px;
        }
        .chapter-grid.view-list .chapter-item {
            flex-direction: row;
            align-items: center;
            padding: 12px 20px;
        }
        .chapter-grid.view-list .chapter-icon {
            font-size: 1.2rem;
            width: 40px;
        }
        
        /* ปุ่ม Tab เลือกโหมดลงนิยาย */
        .tab-btn {
            padding: 10px 20px;
            border: none;
            background: transparent;
            font-weight: 600;
            color: var(--text-muted);
            cursor: pointer;
            border-bottom: 2px solid transparent;
            transition: 0.2s;
        }
        .tab-btn:hover { color: var(--primary); }
        .tab-btn.active {
            color: var(--primary);
            border-bottom-color: var(--primary);
        }

        /* ปุ่มเลือก View Active */
        .view-btn.active {
            background-color: var(--primary);
            color: white;
            border-color: var(--primary);
        }
    </style>
</head>
<body>
    <div id="loadingOverlay" style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.8); z-index:999; flex-direction:column; justify-content:center; align-items:center; color:white; backdrop-filter:blur(5px);">
        <i class="fa-solid fa-spinner fa-spin" style="font-size: 3em; margin-bottom: 20px;"></i>
        <h3>กำลังประมวลผล...</h3>
    </div>

    <header class="main-header">
        <div class="container d-flex justify-between align-center">
            <a href="/" class="btn btn-outline btn-sm" style="border:none; padding-left:0;"><i class="fa-solid fa-chevron-left"></i> กลับหน้าหลัก</a>
            <button onclick="toggleTheme()" class="btn btn-outline btn-sm" style="border-radius:50%; width:40px; height:40px; padding:0;"><i class="fa-solid fa-moon"></i></button>
        </div>
    </header>

    <div class="container" style="padding-top: 40px; padding-bottom: 60px;">
        
        <div class="novel-header-grid">
            <div>
                <img src="<%= novel.imageUrl || 'https://via.placeholder.com/300x450?text=No+Cover' %>" class="novel-cover-large" alt="Cover">
                
                <div class="d-flex gap-2" style="margin-top: 20px;">
                    <button class="btn btn-primary w-100"><i class="fa-solid fa-book-open"></i> เริ่มอ่าน</button>
                    <button class="btn btn-outline w-100"><i class="fa-regular fa-bookmark"></i></button>
                </div>
            </div>
            
            <div>
                <h1 style="font-size: 2rem; margin: 0 0 10px 0; line-height: 1.2;"><%= novel.title %></h1>
                <div style="font-size: 1.1rem; color: var(--text-muted); margin-bottom: 25px; font-weight: 300;">
                    <%= novel.originalTitle || "" %>
                </div>

                <div class="novel-info-table">
                    <span class="info-label">ผู้แต่ง</span> <span><%= novel.author %></span>
                    <span class="info-label">ผู้วาด</span> <span><%= novel.artist %></span>
                    <span class="info-label">สถานะ</span> 
                    <span style="color: <%= novel.status === 'Completed' ? 'var(--success)' : 'var(--primary)' %>; font-weight:700;"><%= novel.status %></span>
                    <span class="info-label">ยอดวิว</span> <span><%= (novel.views || 0).toLocaleString() %> ครั้ง</span>
                </div>

                <div class="d-flex gap-2" style="flex-wrap: wrap; margin-bottom: 25px;">
                    <% if(novel.tags && novel.tags.length > 0) { %>
                        <% novel.tags.forEach(tag => { %>
                            <span style="background:var(--bg-input); border:1px solid var(--border); padding:4px 12px; border-radius:20px; font-size:0.85rem; color:var(--text-muted);">#<%= tag %></span>
                        <% }) %>
                    <% } %>
                </div>

                <div style="background: var(--bg-card); padding: 20px; border-radius: var(--radius); border: 1px solid var(--border); line-height: 1.7; color: var(--text-muted);">
                    <h4 style="margin-top:0; color:var(--text-main);">เรื่องย่อ</h4>
                    <%= novel.description || "ไม่มีคำอธิบาย" %>
                    <% if(novel.originalLink) { %>
                        <div style="margin-top: 15px;">
                            <a href="<%= novel.originalLink %>" target="_blank" style="color: var(--accent); font-size: 0.9rem; text-decoration: none;">
                                <i class="fa-solid fa-link"></i> ไปยังต้นฉบับ
                            </a>
                        </div>
                    <% } %>
                </div>

                <% if(isAdmin) { %>
                    <div style="margin-top: 20px;">
                        <a href="/novel/<%= novel._id %>/edit" class="btn btn-outline btn-sm"><i class="fa-solid fa-pen"></i> แก้ไขข้อมูล</a>
                    </div>
                <% } %>
            </div>
        </div>

        <hr style="border: 0; border-top: 1px solid var(--border); margin: 40px 0;">

        <% if (isAdmin) { %>
        <div class="card" style="border: 2px dashed var(--primary); background: rgba(79, 70, 229, 0.02);">
            <div style="display: flex; border-bottom: 1px solid var(--border); margin-bottom: 20px;">
                <button class="tab-btn active" id="tabBtnAuto" onclick="switchTab('auto')">✨ AI แปลอัตโนมัติ</button>
                <button class="tab-btn" id="tabBtnManual" onclick="switchTab('manual')">✍️ เขียน/ลงเอง (Manual)</button>
            </div>

            <form id="form-auto" action="/novel/<%= novel._id %>/chapters" method="POST" enctype="multipart/form-data" onsubmit="showLoading()">
                <input type="hidden" name="mode" value="auto">
                <div class="d-flex gap-2 mb-4">
                    <label style="cursor: pointer;"><input type="radio" name="inputType" value="text" checked onclick="toggleInput('text')"> วางข้อความ</label>
                    <label style="cursor: pointer;"><input type="radio" name="inputType" value="file" onclick="toggleInput('file')"> อัปโหลดไฟล์ .txt</label>
                </div>
                
                <div id="input-text-group">
                    <textarea name="rawText" placeholder="วางเนื้อหาภาษาญี่ปุ่น..." style="height:120px;"></textarea>
                </div>
                <div id="input-file-group" style="display:none;">
                    <input type="file" name="txtFile" accept=".txt">
                </div>

                <button type="submit" class="btn btn-primary w-100" style="margin-top:15px;">แปลและลงทันที</button>
            </form>

            <form id="form-manual" action="/novel/<%= novel._id %>/chapters" method="POST" style="display:none;" onsubmit="showLoading()">
                <input type="hidden" name="mode" value="manual">
                
                <div style="background: var(--bg-card); padding: 15px; border-radius: 8px; border: 1px solid var(--border); margin-bottom: 15px;">
                    <label style="font-weight: 600; font-size: 0.9rem;">Smart Paste (วางเพื่อแกะชื่อตอนอัตโนมัติ):</label>
                    <textarea id="smartPasteInput" style="height: 50px; margin-top: 5px;" placeholder="ตัวอย่าง: ตอนที่ 1 จุดเริ่มต้น..." oninput="detectHeader()"></textarea>
                </div>

                <div class="d-flex gap-2 mb-4">
                    <div style="width: 120px;">
                        <input type="number" step="0.1" id="manualChapterNumber" name="manualChapterNumber" placeholder="เลขตอน" required>
                    </div>
                    <div style="flex: 1;">
                        <input type="text" id="manualTitle" name="manualTitle" placeholder="ชื่อตอน (Title)">
                    </div>
                </div>

                <textarea id="manualTranslated" name="manualTranslated" style="height: 200px;" placeholder="เนื้อหานิยายภาษาไทย..." required></textarea>
                
                <button type="submit" class="btn btn-primary w-100" style="margin-top:15px;">บันทึกเนื้อหา</button>
            </form>
        </div>
        <% } %>

        <div class="d-flex justify-between align-center mb-4">
            <h3 style="margin:0;">สารบัญตอน (<%= chapters.length %>)</h3>
            <div class="d-flex gap-2">
                <button class="btn btn-outline btn-sm view-btn active" id="btn-grid" onclick="setView('grid')"><i class="fa-solid fa-table-cells-large"></i></button>
                <button class="btn btn-outline btn-sm view-btn" id="btn-list" onclick="setView('list')"><i class="fa-solid fa-list"></i></button>
                <button class="btn btn-outline btn-sm" onclick="reverseSort()"><i class="fa-solid fa-sort"></i> เรียง</button>
            </div>
        </div>

        <div id="chapterContainer" class="chapter-grid">
            <% chapters.forEach(chapter => { %>
                <a href="/chapter/<%= chapter._id %>" class="chapter-item" id="chapter-<%= chapter._id %>">
                    <div class="chapter-icon" style="font-size: 1.5rem; color: var(--text-muted); opacity: 0.5;">
                        <i class="fa-solid fa-file-lines"></i>
                    </div>
                    <div style="flex: 1; overflow: hidden;">
                        <div style="font-weight: 600; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">
                            <%= chapter.title.replace(/^ตอนที่ \d+[:\s]*/, '') || `Ep.${chapter.chapterNumber}` %>
                        </div>
                        <div style="font-size: 0.8rem; color: var(--text-muted); margin-top: 2px;">
                            Episode <%= chapter.chapterNumber %> • <%= new Date(chapter.createdAt).toLocaleDateString('th-TH') %>
                        </div>
                    </div>
                    <i class="fa-solid fa-chevron-right" style="font-size: 0.8rem; color: var(--border);"></i>
                </a>
            <% }) %>
        </div>

    </div>

    <script>
        document.documentElement.setAttribute('data-theme', localStorage.getItem('theme') || 'light');
        function toggleTheme() {
            const next = document.documentElement.getAttribute('data-theme') === 'light' ? 'dark' : 'light';
            document.documentElement.setAttribute('data-theme', next);
            localStorage.setItem('theme', next);
        }

        function showLoading() { document.getElementById('loadingOverlay').style.display = 'flex'; }

        // --- Tab Switching Logic ---
        function switchTab(mode) {
            document.getElementById('form-auto').style.display = mode === 'auto' ? 'block' : 'none';
            document.getElementById('form-manual').style.display = mode === 'manual' ? 'block' : 'none';
            
            document.getElementById('tabBtnAuto').classList.toggle('active', mode === 'auto');
            document.getElementById('tabBtnManual').classList.toggle('active', mode === 'manual');
        }

        function toggleInput(type) {
            document.getElementById('input-text-group').style.display = type === 'text' ? 'block' : 'none';
            document.getElementById('input-file-group').style.display = type === 'file' ? 'block' : 'none';
        }

        // --- View Switching Logic (Grid/List) ---
        function setView(mode) {
            const container = document.getElementById('chapterContainer');
            const btnGrid = document.getElementById('btn-grid');
            const btnList = document.getElementById('btn-list');

            if(mode === 'list') {
                container.classList.add('view-list');
                btnList.classList.add('active');
                btnGrid.classList.remove('active');
                localStorage.setItem('chapterView', 'list');
            } else {
                container.classList.remove('view-list');
                btnGrid.classList.add('active');
                btnList.classList.remove('active');
                localStorage.setItem('chapterView', 'grid');
            }
        }

        // --- Manual Form Header Detection ---
        function detectHeader() {
            const input = document.getElementById('smartPasteInput').value;
            if (!input.trim()) return;
            const lines = input.split('\n');
            // Regex จับแพทเทิร์น "ตอนที่ 1 ชื่อตอน..."
            const match = lines[0].trim().match(/^(?:ตอนที่|บทที่|Chapter|Ep|Episode|第)?\s*(\d+(\.\d+)?)\s*[:：.\s]?\s*(.*)$/i);
            
            if (match) {
                document.getElementById('manualChapterNumber').value = match[1];
                document.getElementById('manualTitle').value = match[3] ? `ตอนที่ ${match[1]} : ${match[3]}` : `ตอนที่ ${match[1]}`;
                // เอาบรรทัดแรกออก แล้วเอาที่เหลือใส่ในเนื้อหา
                document.getElementById('manualTranslated').value = lines.slice(1).join('\n').trim();
            } else {
                document.getElementById('manualTitle').value = lines[0];
                document.getElementById('manualTranslated').value = lines.slice(1).join('\n').trim();
            }
        }

        function reverseSort() {
            const container = document.getElementById('chapterContainer');
            const items = Array.from(container.children);
            items.reverse().forEach(item => container.appendChild(item));
        }

        // Restore View on Load
        document.addEventListener('DOMContentLoaded', () => {
            const savedView = localStorage.getItem('chapterView') || 'grid';
            setView(savedView);
        });
    </script>
</body>
</html>