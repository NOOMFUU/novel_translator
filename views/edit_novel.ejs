<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>แก้ไข: <%= novel.title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <div class="container" style="padding-top: 40px; padding-bottom: 40px;">
        <div class="card">
            <h2 class="mb-4">แก้ไขข้อมูลนิยาย</h2>
            <form action="/novel/<%= novel._id %>?_method=PUT" method="POST">
                
                <div class="d-flex gap-2 mb-4" style="flex-wrap: wrap;">
                    <div style="flex: 1; min-width: 300px;">
                        <label class="text-muted">ชื่อเรื่อง (ไทย)</label>
                        <input type="text" name="title" value="<%= novel.title %>" required>
                    </div>
                    <div style="flex: 1; min-width: 300px;">
                        <label class="text-muted">ชื่อเรื่องเดิม (ญี่ปุ่น/อังกฤษ)</label>
                        <input type="text" name="originalTitle" value="<%= novel.originalTitle %>">
                    </div>
                </div>

                <div class="d-flex gap-2 mb-4" style="flex-wrap: wrap;">
                    <div style="flex: 1;">
                        <label class="text-muted">ผู้แต่ง (Author)</label>
                        <input type="text" name="author" value="<%= novel.author %>">
                    </div>
                    <div style="flex: 1;">
                        <label class="text-muted">นักวาด (Artist)</label>
                        <input type="text" name="artist" value="<%= novel.artist %>">
                    </div>
                    <div style="flex: 1;">
                        <label class="text-muted">สถานะ</label>
                        <select name="status" style="width: 100%; padding: 10px; border-radius: 8px; border: 1px solid var(--border); background: var(--bg-body); color: var(--text-main);">
                            <option value="On Going" <%= novel.status === 'On Going' ? 'selected' : '' %>>On Going</option>
                            <option value="Completed" <%= novel.status === 'Completed' ? 'selected' : '' %>>Completed</option>
                            <option value="Hiatus" <%= novel.status === 'Hiatus' ? 'selected' : '' %>>Hiatus</option>
                        </select>
                    </div>
                </div>

                <div class="mb-4">
                    <label class="text-muted">ลิงก์รูปปก (Image URL)</label>
                    <input type="text" name="imageUrl" value="<%= novel.imageUrl %>" placeholder="https://...">
                </div>
                <div class="mb-4">
                    <label class="text-muted">ลิงก์เว็บต้นฉบับ (Original Link)</label>
                    <input type="text" name="originalLink" value="<%= novel.originalLink %>" placeholder="https://kakuyomu.jp/...">
                </div>

                <div class="mb-4">
                    <label class="text-muted">Tags (คั่นด้วยลูกน้ำ , )</label>
                    <input type="text" name="tags" value="<%= novel.tags.join(', ') %>" placeholder="Comedy, Romance, School Life">
                </div>

                <div class="mb-4">
                    <label class="text-muted">เรื่องย่อ</label>
                    <textarea name="description" style="height: 120px;"><%= novel.description %></textarea>
                </div>

                <hr style="margin: 30px 0; border: 0; border-top: 1px solid var(--border);">

                <div style="background: rgba(59, 130, 246, 0.1); padding: 20px; border-radius: 8px; margin-bottom: 20px;">
                    <h4 style="margin-top:0; color: var(--accent);">การตั้งค่า AI</h4>
                    <div class="mb-4">
                        <label class="text-muted">System Prompt / สไตล์การแปล</label>
                        <input type="text" name="customPrompt" value="<%= novel.customPrompt %>">
                    </div>
                    <div>
                        <label class="text-muted">สมุดคำศัพท์ (Japanese = Thai)</label>
                        <textarea name="glossary" style="height: 100px; font-family: monospace;"><%= novel.glossary %></textarea>
                    </div>
                </div>

                <div class="d-flex justify-between">
                    <a href="/novel/<%= novel._id %>" class="btn btn-outline">ยกเลิก</a>
                    <button type="submit" class="btn btn-primary">บันทึกข้อมูล</button>
                </div>
            </form>
        </div>
    </div>
    <script>
        document.documentElement.setAttribute('data-theme', localStorage.getItem('theme') || 'light');
    </script>
</body>
</html>