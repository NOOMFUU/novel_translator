<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>NovelHub - นิยายแปลคุณภาพ</title>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="/css/style.css">

    <script>
        // Prevent Flash of Unstyled Content (FOUC)
        (function() {
            const savedTheme = localStorage.getItem('theme') || 'light';
            document.documentElement.setAttribute('data-theme', savedTheme);
        })();
    </script>

    <style>
        /* --- DESIGN SYSTEM (Premium Theme) --- */
        :root {
            --header-height: 64px;
            --bottom-nav-height: 70px; /* สูงขึ้นนิดเพื่อให้แตะง่าย */
            --container-max: 1240px;
            
            /* Color Palette (Modern Indigo/Violet) */
            --primary: #6366f1;
            --primary-dark: #4338ca;
            --primary-light: #e0e7ff;
            --secondary: #ec4899; /* Pink accent */
            
            --bg-body: #f1f5f9;
            --bg-surface: #ffffff;
            --bg-surface-glass: rgba(255, 255, 255, 0.85);
            
            --text-main: #0f172a;
            --text-muted: #64748b;
            --text-light: #94a3b8;
            
            --border: #e2e8f0;
            --border-glass: rgba(255, 255, 255, 0.5);
            
            /* Shadows */
            --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.05), 0 4px 6px -2px rgba(0, 0, 0, 0.025);
            --shadow-float: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            
            --radius-sm: 8px;
            --radius-md: 12px;
            --radius-lg: 20px;
            --radius-full: 9999px;

            --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
        }

        [data-theme="dark"] {
            --bg-body: #020617; /* Very dark blue */
            --bg-surface: #1e293b;
            --bg-surface-glass: rgba(30, 41, 59, 0.85);
            --text-main: #f8fafc;
            --text-muted: #cbd5e1;
            --border: #334155;
            --border-glass: rgba(255, 255, 255, 0.05);
            --primary-light: rgba(99, 102, 241, 0.15);
        }

        /* --- GLOBAL RESET & TYPOGRAPHY --- */
        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
        
        body {
            font-family: 'Sarabun', sans-serif;
            background-color: var(--bg-body);
            color: var(--text-main);
            margin: 0;
            padding-top: var(--header-height);
            padding-bottom: 90px;
            opacity: 0; animation: pageFadeIn 0.6s var(--ease-out) forwards; /* Fade In Effect */
        }
        
        @keyframes pageFadeIn { to { opacity: 1; } }

        a { text-decoration: none; color: inherit; }
        ul { list-style: none; padding: 0; margin: 0; }

        .container {
            max-width: var(--container-max);
            margin: 0 auto;
            padding: 0 24px;
        }

        /* --- HEADER (Glassmorphism) --- */
        .main-header {
            position: fixed; top: 0; left: 0; right: 0;
            height: var(--header-height);
            background: var(--bg-surface-glass);
            backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);
            border-bottom: 1px solid var(--border);
            z-index: 1000;
            transition: all 0.3s ease;
        }
        
        .header-content {
            display: flex; justify-content: space-between; align-items: center;
            height: 100%;
            width: 100%;
        }

        .logo {
            font-size: 1.5rem; font-weight: 800; letter-spacing: -0.5px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
            display: flex; align-items: center; gap: 10px;
        }

        /* Search Bar Desktop */
        .search-container {
            flex: 1; max-width: 480px; margin: 0 40px;
            position: relative; display: none;
        }
        .search-input {
            width: 100%; padding: 10px 20px 10px 48px;
            border-radius: var(--radius-full);
            border: 2px solid transparent;
            background: var(--bg-body);
            color: var(--text-main); font-size: 0.95rem; font-family: inherit;
            transition: all 0.3s var(--ease-out);
        }
        .search-input:focus {
            background: var(--bg-surface);
            border-color: var(--primary);
            box-shadow: 0 0 0 4px var(--primary-light);
            outline: none;
        }
        .search-icon {
            position: absolute; left: 18px; top: 50%; transform: translateY(-50%);
            color: var(--text-muted); pointer-events: none;
        }

        /* --- HERO SECTION (Dynamic Blur) --- */
        .hero-section {
            margin: 24px 0 40px;
            border-radius: var(--radius-lg);
            position: relative; overflow: hidden;
            box-shadow: var(--shadow-lg);
            min-height: 280px; display: flex; align-items: center;
        }
        
        .hero-backdrop {
            position: absolute; inset: 0;
            background-size: cover; background-position: center;
            filter: blur(40px) brightness(0.6); /* Blur Effect */
            transform: scale(1.1); /* Prevent blur edges */
            z-index: 0;
        }
        [data-theme="light"] .hero-backdrop { filter: blur(50px) brightness(0.9) saturate(1.2); opacity: 0.8; }
        
        .hero-content {
            position: relative; z-index: 2;
            padding: 30px 40px; width: 100%;
            display: flex; gap: 40px; align-items: center;
            color: white;
            text-shadow: 0 2px 10px rgba(0,0,0,0.3);
        }
        [data-theme="light"] .hero-content { color: var(--text-main); text-shadow: none; }

        .hero-cover {
            width: 160px; aspect-ratio: 2/3; object-fit: cover;
            border-radius: var(--radius-md);
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
            transform: rotate(-3deg) translateY(0);
            transition: transform 0.4s var(--ease-out);
            border: 4px solid rgba(255,255,255,0.2);
        }
        .hero-section:hover .hero-cover { transform: rotate(0deg) translateY(-5px); }

        .hero-details h1 {
            font-size: 2rem; margin: 10px 0 15px; line-height: 1.2;
            display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;
        }
        
        .badge {
            display: inline-flex; align-items: center; padding: 4px 10px;
            border-radius: var(--radius-sm); font-size: 0.75rem; font-weight: 700;
            letter-spacing: 0.5px; text-transform: uppercase;
        }
        .badge-glass {
            background: rgba(255,255,255,0.2); backdrop-filter: blur(8px); border: 1px solid rgba(255,255,255,0.3);
        }
        [data-theme="light"] .badge-glass { background: rgba(0,0,0,0.05); border-color: rgba(0,0,0,0.05); color: var(--text-main); }

        /* --- QUICK FILTERS (Scrollable) --- */
        .quick-filter-bar {
            display: flex; gap: 12px; margin-bottom: 24px;
            overflow-x: auto; padding-bottom: 8px; /* For scrollbar space */
            scrollbar-width: none; -ms-overflow-style: none; /* Hide Scrollbar */
        }
        .quick-filter-bar::-webkit-scrollbar { display: none; }

        .q-chip {
            white-space: nowrap; padding: 8px 16px;
            border-radius: var(--radius-full);
            background: var(--bg-surface); border: 1px solid var(--border);
            color: var(--text-muted); font-size: 0.9rem; font-weight: 500;
            transition: all 0.2s; user-select: none;
        }
        .q-chip:hover { transform: translateY(-2px); border-color: var(--primary); color: var(--primary); }
        .q-chip.active {
            background: var(--primary); border-color: var(--primary); color: white;
            box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3);
        }

        /* --- NOVEL GRID --- */
        .grid-header {
            display: flex; justify-content: space-between; align-items: flex-end;
            margin-bottom: 20px;
        }
        .grid-title { font-size: 1.25rem; font-weight: 700; margin: 0; display: flex; align-items: center; gap: 8px; }

        .novel-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
            gap: 24px;
        }

        .novel-card {
            background: transparent;
            position: relative; transition: 0.3s var(--ease-out);
            group: card;
        }
        
        .card-cover-wrapper {
            position: relative; border-radius: var(--radius-md); overflow: hidden;
            aspect-ratio: 2/3; box-shadow: var(--shadow-md);
            margin-bottom: 12px; background: var(--bg-body);
        }
        .card-cover-wrapper::after { /* Shine Effect on Hover */
            content: ''; position: absolute; inset: 0;
            background: linear-gradient(to bottom, transparent 0%, rgba(0,0,0,0.05) 100%);
            opacity: 0; transition: opacity 0.3s;
        }
        .novel-card:hover .card-cover-wrapper::after { opacity: 1; }
        .novel-card:hover .card-cover-wrapper { transform: translateY(-4px); box-shadow: var(--shadow-lg); }

        .card-img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s; }
        .novel-card:hover .card-img { transform: scale(1.08); }

        .card-status {
            position: absolute; top: 8px; right: 8px;
            padding: 4px 8px; border-radius: 6px;
            font-size: 0.65rem; font-weight: 800; color: white;
            backdrop-filter: blur(8px); box-shadow: 0 2px 8px rgba(0,0,0,0.2);
            text-transform: uppercase; z-index: 2;
        }
        .st-on { background: rgba(16, 185, 129, 0.9); }
        .st-end { background: rgba(239, 68, 68, 0.9); }
        .st-pause { background: rgba(245, 158, 11, 0.9); }

        .card-info h3 {
            font-size: 1rem; font-weight: 700; margin: 0 0 4px 0;
            line-height: 1.4; height: 2.8em;
            display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;
            transition: color 0.2s;
        }
        .novel-card:hover .card-info h3 { color: var(--primary); }

        .card-meta {
            display: flex; justify-content: space-between; align-items: center;
            font-size: 0.8rem; color: var(--text-muted);
        }
        .ep-badge { color: var(--primary); font-weight: 600; display: flex; align-items: center; gap: 4px; }

        /* --- MOBILE FLOATING BUTTON (FAB) --- */
        .fab-filter {
            position: fixed; bottom: calc(var(--bottom-nav-height) + 20px); right: 20px;
            width: 56px; height: 56px; border-radius: 50%;
            background: var(--primary); color: white;
            box-shadow: var(--shadow-float);
            display: flex; align-items: center; justify-content: center;
            font-size: 1.2rem; cursor: pointer; z-index: 900;
            border: none; transition: transform 0.2s;
            display: none; /* Hidden on Desktop */
        }
        .fab-filter:active { transform: scale(0.9); }
        .fab-badge {
            position: absolute; top: 0; right: 0;
            width: 18px; height: 18px; background: var(--secondary);
            border-radius: 50%; border: 2px solid var(--bg-body);
            display: block;
        }

        /* --- DRAWER (FILTER) --- */
        .drawer-overlay {
            position: fixed; inset: 0; background: rgba(0,0,0,0.6);
            z-index: 2000; opacity: 0; visibility: hidden; transition: 0.3s;
            backdrop-filter: blur(4px);
        }
        .drawer-overlay.open { opacity: 1; visibility: visible; }
        
        .drawer-panel {
            position: fixed; bottom: 0; left: 0; right: 0;
            background: var(--bg-surface);
            border-radius: 24px 24px 0 0;
            padding: 24px; z-index: 2001;
            transform: translateY(100%); transition: 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            max-height: 85vh; overflow-y: auto;
            box-shadow: 0 -10px 40px rgba(0,0,0,0.2);
        }
        .drawer-overlay.open .drawer-panel { transform: translateY(0); }

        .drawer-handle {
            width: 40px; height: 4px; background: var(--border);
            border-radius: 2px; margin: 0 auto 20px; opacity: 0.5;
        }

        /* --- MOBILE NAV --- */
        .bottom-nav {
            display: none;
            position: fixed; bottom: 0; left: 0; right: 0;
            height: var(--bottom-nav-height);
            background: var(--bg-surface-glass);
            backdrop-filter: blur(20px);
            border-top: 1px solid var(--border-glass);
            z-index: 1000;
            justify-content: space-around; align-items: center;
            padding-bottom: env(safe-area-inset-bottom);
        }
        
        .b-nav-item {
            display: flex; flex-direction: column; align-items: center; gap: 4px;
            color: var(--text-muted); font-size: 0.7rem; font-weight: 500;
            width: 100%; height: 100%; justify-content: center;
            transition: color 0.2s; position: relative;
        }
        .b-nav-item i { font-size: 1.3rem; margin-bottom: 2px; transition: transform 0.2s; }
        .b-nav-item.active { color: var(--primary); }
        .b-nav-item.active i { transform: translateY(-2px); }

        /* --- RESPONSIVE MEDIA QUERIES --- */
        @media (min-width: 769px) {
            .search-container { display: block; }
            .mobile-only { display: none !important; }
            
            /* Center Modal on Desktop */
            .drawer-panel {
                top: 50%; left: 50%; bottom: auto; right: auto;
                transform: translate(-50%, -50%) scale(0.9);
                width: 480px; max-width: 90%; border-radius: 24px;
                opacity: 0;
            }
            .drawer-overlay.open .drawer-panel {
                transform: translate(-50%, -50%) scale(1); opacity: 1;
            }
            .drawer-handle { display: none; }
        }

        @media (max-width: 768px) {
            .container { padding: 0 16px; }
            .hero-section {
                flex-direction: column; text-align: left;
                align-items: flex-start; padding: 20px;
                min-height: auto;
                margin-top: 16px;
            }
            .hero-content { padding: 0; gap: 20px; flex-direction: row; }
            .hero-cover { width: 90px; border-width: 2px; transform: none; }
            .hero-details h1 { font-size: 1.4rem; }
            
            .novel-grid {
                grid-template-columns: repeat(2, 1fr); /* 2 Column Mobile */
                gap: 12px; row-gap: 24px;
            }
            .novel-title { font-size: 0.9rem; }
            
            .bottom-nav { display: flex; }
            .fab-filter { display: flex; }
            .desktop-only { display: none !important; }
            
            .q-chip { font-size: 0.8rem; padding: 6px 14px; }
        }
    </style>
</head>
<body>

    <%
        // URL Builder Helper
        const buildUrl = (overrides = {}) => {
            let params = new URLSearchParams();
            if(locals.query) params.append('q', locals.query);
            
            let tags = locals.currentTags ? [...locals.currentTags] : [];
            if(overrides.clearTags) tags = [];
            else if(overrides.toggleTag) {
                if(tags.includes(overrides.toggleTag)) tags = tags.filter(t => t !== overrides.toggleTag);
                else tags.push(overrides.toggleTag);
            }

            if(!overrides.clearTags) tags.forEach(t => params.append('tag', t));

            const cat = overrides.category !== undefined ? overrides.category : locals.currentCategory;
            if(cat) params.append('category', cat);
            
            const stat = overrides.status !== undefined ? overrides.status : locals.currentStatus;
            if(stat) params.append('status', stat);
            
            const srt = overrides.sort !== undefined ? overrides.sort : locals.sort;
            if(srt) params.append('sort', srt);
            
            const pg = overrides.page !== undefined ? overrides.page : 1;
            params.append('page', pg);

            return '/?' + params.toString();
        }
        
        // Count active filters for badge
        let filterCount = (currentCategory ? 1 : 0) + (currentStatus ? 1 : 0) + (currentTags ? currentTags.length : 0);
    %>

    <header class="main-header">
        <div class="container header-content">
            <a href="/" class="logo">
                <i class="fa-solid fa-book-open-reader"></i>
                <span>NovelHub</span>
            </a>

            <form action="/" method="GET" class="search-container">
                <i class="fa-solid fa-magnifying-glass search-icon"></i>
                <input type="text" name="q" class="search-input" placeholder="ค้นหานิยาย, ผู้แต่ง..." value="<%= locals.query || '' %>">
            </form>

            <div style="display:flex; gap:8px; align-items:center;">
                <button onclick="toggleTheme()" style="width:40px; height:40px; border-radius:50%; border:none; background:transparent; color:var(--text-main); cursor:pointer; font-size:1.1rem;">
                    <i class="fa-solid fa-moon"></i>
                </button>
                
                <button class="mobile-only" onclick="document.getElementById('mobileSearch').classList.add('open')" style="width:40px; height:40px; border-radius:50%; border:none; background:transparent; color:var(--text-main); cursor:pointer; font-size:1.1rem;">
                    <i class="fa-solid fa-magnifying-glass"></i>
                </button>

                <% if(user) { %>
                    <a href="/favorites" class="desktop-only" style="margin:0 10px; font-weight:600; font-size:0.9rem;">
                        <i class="fa-regular fa-heart" style="color:var(--secondary);"></i> คลัง
                    </a>
                    <a href="/logout" class="desktop-only" style="color:var(--text-muted); font-size:0.9rem;">ออกระบบ</a>
                <% } else { %>
                    <a href="/login" class="desktop-only" style="background:var(--primary); color:white; padding:8px 20px; border-radius:var(--radius-full); font-size:0.9rem; font-weight:600;">เข้าสู่ระบบ</a>

                    <a href="/register" class="desktop-only">สมัครสมาชิก</a>
                <% } %>
            </div>
        </div>
    </header>

    <div class="container">
        
        <% if (!locals.query && !locals.currentCategory && (!locals.currentTags || locals.currentTags.length === 0) && !locals.currentStatus && currentPage === 1) { %> 
            <% if (locals.recommended) { %>
            <div class="hero-section">
                <div class="hero-backdrop" style="background-image: url('<%= recommended.imageUrl || defaultCover %>');"></div>
                <div class="hero-content">
                    <img src="<%= recommended.imageUrl || defaultCover %>" class="hero-cover" onerror="this.onerror=null;this.src='<%= defaultCover %>';">
                    <div class="hero-details">
                        <span class="badge badge-glass"><i class="fa-solid fa-crown" style="color:#fbbf24; margin-right:4px;"></i> Recommended</span>
                        <h1><%= recommended.title %></h1>
                        <div style="display:flex; gap:12px; font-size:0.9rem; opacity:0.9; flex-wrap:wrap;">
                            <span><i class="fa-solid fa-pen-nib"></i> <%= recommended.author %></span>
                            <span><i class="fa-solid fa-layer-group"></i> <%= recommended.categories[0] || 'General' %></span>
                        </div>
                        <a href="/novel/<%= recommended._id %>" style="display:inline-block; margin-top:16px; background:var(--primary); color:white; padding:10px 24px; border-radius:var(--radius-full); font-weight:600; box-shadow:0 4px 15px rgba(99,102,241,0.4);">
                            อ่านเลย
                        </a>
                    </div>
                </div>
            </div>
            <% } %>
        <% } %>

        <div class="grid-header">
            <h2 class="grid-title">
                <% if(locals.query) { %> <i class="fa-solid fa-magnifying-glass"></i> ผลการค้นหา "<%= query %>"
                <% } else if(currentCategory) { %> <i class="fa-solid fa-folder-open"></i> หมวด: <%= currentCategory %>
                <% } else { %> <i class="fa-solid fa-fire" style="color:#f59e0b;"></i> นิยายล่าสุด <% } %>
            </h2>
            
            <button class="desktop-only" onclick="openDrawer()" style="background:var(--bg-surface); border:1px solid var(--border); padding:8px 16px; border-radius:var(--radius-full); cursor:pointer; font-family:inherit; color:var(--text-main); font-weight:500;">
                <i class="fa-solid fa-sliders"></i> ตัวกรอง <%= filterCount > 0 ? `(${filterCount})` : '' %>
            </button>
        </div>

        <div class="quick-filter-bar">
            <a href="/" class="q-chip <%= filterCount === 0 ? 'active' : '' %>">
                ทั้งหมด
            </a>
            
            
            <button onclick="openDrawer()" class="q-chip" style="border-style:dashed;">
                <i class="fa-solid fa-plus"></i> เพิ่มเติม
            </button>
        </div>

        <% if (novels.length === 0) { %>
            <div style="text-align:center; padding: 60px 20px; color:var(--text-muted);">
                <i class="fa-solid fa-inbox" style="font-size:3rem; opacity:0.3; margin-bottom:12px;"></i>
                <p>ไม่พบนิยายตามเงื่อนไข</p>
                <a href="/" style="color:var(--primary); text-decoration:underline;">ล้างตัวกรอง</a>
            </div>
        <% } %>

        <div class="novel-grid">
            <% novels.forEach(novel => { %>
                <a href="/novel/<%= novel._id %>" class="novel-card">
                    <div class="card-cover-wrapper">
                        <% 
                            let stClass = 'st-on'; let stText = 'ONGOING';
                            if(novel.status === 'Completed') { stClass = 'st-end'; stText = 'COMPLETE'; }
                            else if(novel.status === 'Hiatus') { stClass = 'st-pause'; stText = 'PAUSE'; }
                        %>
                        <div class="card-status <%= stClass %>"><%= stText %></div>
                        <img src="<%= novel.imageUrl || defaultCover %>" class="card-img" loading="lazy" onerror="this.onerror=null;this.src='<%= defaultCover %>';">
                    </div>
                    
                    <div class="card-info">
                        <div class="card-meta" style="margin-bottom:4px;">
                            <span style="font-size:0.7rem; background:var(--bg-surface); border:1px solid var(--border); padding:1px 6px; border-radius:4px;">
                                <%= (novel.categories && novel.categories[0]) || 'General' %>
                            </span>
                            <span><i class="fa-regular fa-eye"></i> <%= (novel.views || 0).toLocaleString() %></span>
                        </div>
                        <h3><%= novel.title %></h3>
                        <div class="card-meta">
                            <% if(novel.lastChapter && novel.lastChapter.chapterNumber) { %>
                                <span class="ep-badge"><i class="fa-solid fa-bolt"></i> Ep.<%= novel.lastChapter.chapterNumber %></span>
                            <% } else { %>
                                <span>-</span>
                            <% } %>
                            <span style="font-size:0.7rem;"><%= new Date(novel.updatedAt).toLocaleDateString('th-TH', {day:'2-digit', month:'2-digit', year:'2-digit'}) %></span>
                        </div>
                    </div>
                </a>
            <% }) %>
        </div>

        <% if (totalPages > 1) { %>
            <div class="pagination-wrapper">
                <a href="<%= currentPage > 1 ? buildUrl({page: currentPage - 1}) : 'javascript:void(0)' %>" 
                   class="page-nav-btn <%= currentPage <= 1 ? 'disabled' : '' %>">
                    <i class="fa-solid fa-chevron-left"></i>
                </a>

                <form action="/" method="GET" class="page-input-group">
                    <% if(locals.query) { %><input type="hidden" name="q" value="<%= query %>"><% } %>
                    <% if(locals.currentCategory) { %><input type="hidden" name="category" value="<%= currentCategory %>"><% } %>
                    <% if(locals.currentStatus) { %><input type="hidden" name="status" value="<%= currentStatus %>"><% } %>
                    <% if(locals.sort) { %><input type="hidden" name="sort" value="<%= sort %>"><% } %>
                    <% if(locals.currentTags && currentTags.length > 0) { 
                         currentTags.forEach(t => { %>
                            <input type="hidden" name="tag" value="<%= t %>">
                    <%   }); 
                       } %>
                    
                    <span class="page-label">หน้า</span>
                    <input type="number" name="page" value="<%= currentPage %>" min="1" max="<%= totalPages %>" class="page-input" onfocus="this.select()" autocomplete="off">
                    <span class="page-label">/ <%= totalPages %></span>
                </form>

                <a href="<%= currentPage < totalPages ? buildUrl({page: currentPage + 1}) : 'javascript:void(0)' %>" 
                   class="page-nav-btn <%= currentPage >= totalPages ? 'disabled' : '' %>">
                    <i class="fa-solid fa-chevron-right"></i>
                </a>
            </div>
        <% } %>

    </div>

    <button class="fab-filter" onclick="openDrawer()">
        <i class="fa-solid fa-sliders"></i>
        <% if(filterCount > 0) { %> <span class="fab-badge"></span> <% } %>
    </button>

    <div class="drawer-overlay" id="filterDrawer">
        <div class="drawer-panel">
            <div class="drawer-handle"></div>
            
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:20px;">
                <h3 style="margin:0; font-size:1.2rem;">ตัวกรองค้นหา</h3>
                <button onclick="closeDrawer()" style="background:none; border:none; font-size:1.2rem; color:var(--text-muted); cursor:pointer;"><i class="fa-solid fa-xmark"></i></button>
            </div>

            <form action="/" method="GET">
                <% if(locals.query) { %> <input type="hidden" name="q" value="<%= query %>"> <% } %>
                
                <div style="margin-bottom:20px;">
                    <label style="display:block; font-weight:600; margin-bottom:10px;">เรียงลำดับ</label>
                    <div style="display:flex; flex-wrap:wrap; gap:8px;">
                        <label class="q-chip <%= sort === 'latest' ? 'active' : '' %>">
                            <input type="radio" name="sort" value="latest" <%= sort === 'latest' ? 'checked' : '' %> hidden> ล่าสุด
                        </label>
                        <label class="q-chip <%= sort === 'popular' ? 'active' : '' %>">
                            <input type="radio" name="sort" value="popular" <%= sort === 'popular' ? 'checked' : '' %> hidden> ยอดนิยม
                        </label>
                        <label class="q-chip <%= sort === 'oldest' ? 'active' : '' %>">
                            <input type="radio" name="sort" value="oldest" <%= sort === 'oldest' ? 'checked' : '' %> hidden> เก่าสุด
                        </label>
                        <label class="q-chip <%= sort === 'az' ? 'active' : '' %>">
                            <input type="radio" name="sort" value="az" <%= sort === 'az' ? 'checked' : '' %> hidden> A-Z
                        </label>
                    </div>
                </div>

                <div style="margin-bottom:20px;">
                    <label style="display:block; font-weight:600; margin-bottom:10px;">สถานะ</label>
                    <select name="status" style="width:100%; padding:12px; border-radius:var(--radius-md); border:1px solid var(--border); background:var(--bg-body); color:var(--text-main); font-family:inherit;">
                        <option value="">ทั้งหมด</option>
                        <option value="On Going" <%= currentStatus === 'On Going' ? 'selected' : '' %>>On Going (กำลังแปล)</option>
                        <option value="Completed" <%= currentStatus === 'Completed' ? 'selected' : '' %>>Completed (จบแล้ว)</option>
                        <option value="Hiatus" <%= currentStatus === 'Hiatus' ? 'selected' : '' %>>Hiatus (ดอง/พัก)</option>
                    </select>
                </div>

                <div style="margin-bottom:20px;">
                    <label style="display:block; font-weight:600; margin-bottom:10px;">หมวดหมู่</label>
                    <select name="category" style="width:100%; padding:12px; border-radius:var(--radius-md); border:1px solid var(--border); background:var(--bg-body); color:var(--text-main); font-family:inherit;">
                        <option value="">ทั้งหมด</option>
                        <% categories.forEach(cat => { %>
                            <option value="<%= cat %>" <%= currentCategory === cat ? 'selected' : '' %>><%= cat %></option>
                        <% }) %>
                    </select>
                </div>

                <div style="margin-bottom:24px;">
                    <label style="display:block; font-weight:600; margin-bottom:10px;">Tags (เลือกได้หลายอัน)</label>
                    <div style="display:flex; flex-wrap:wrap; gap:8px;">
                        <% if(locals.allTags) { %>
                            <% allTags.forEach(tag => { %>
                                <% const isActive = currentTags && currentTags.includes(tag); %>
                                <label class="q-chip <%= isActive ? 'active' : '' %>" style="cursor:pointer;">
                                    <input type="checkbox" name="tag" value="<%= tag %>" <%= isActive ? 'checked' : '' %> hidden>
                                    #<%= tag %>
                                </label>
                            <% }) %>
                        <% } %>
                    </div>
                </div>

                <button type="submit" style="width:100%; padding:14px; background:var(--primary); color:white; border:none; border-radius:var(--radius-md); font-weight:700; font-size:1rem; cursor:pointer; box-shadow:0 4px 15px rgba(99,102,241,0.3);">
                    ค้นหา
                </button>
            </form>
        </div>
    </div>

    <div class="drawer-overlay" id="mobileSearch" style="align-items:flex-start;">
        <div style="width:100%; background:var(--bg-surface); padding:16px; border-radius:0 0 20px 20px; box-shadow:0 4px 20px rgba(0,0,0,0.1);">
            <form action="/" method="GET" style="display:flex; gap:10px;">
                <input type="text" name="q" class="search-input" placeholder="ค้นหา..." value="<%= locals.query || '' %>" style="background:var(--bg-body);" autofocus>
                <button type="button" onclick="document.getElementById('mobileSearch').classList.remove('open')" style="background:none; border:none; color:var(--text-main);">ปิด</button>
            </form>
        </div>
    </div>

    <nav class="bottom-nav">
        <a href="/" class="b-nav-item active">
            <i class="fa-solid fa-house"></i> หน้าแรก
        </a>
        <a href="/favorites" class="b-nav-item">
            <i class="fa-regular fa-heart"></i> คลัง
        </a>
        <a href="<%= user ? '/logout' : '/login' %>" class="b-nav-item">
            <i class="<%= user ? 'fa-solid fa-arrow-right-from-bracket' : 'fa-regular fa-user' %>"></i> <%= user ? 'ออกระบบ' : 'เข้าสู่ระบบ/สมัครสมาชิก' %>
        </a>
    </nav>

    <script>
        // Modal Logic
        const drawer = document.getElementById('filterDrawer');
        
        function openDrawer() {
            drawer.classList.add('open');
            document.body.style.overflow = 'hidden';
        }
        
        function closeDrawer() {
            drawer.classList.remove('open');
            document.body.style.overflow = '';
        }

        drawer.addEventListener('click', (e) => {
            if(e.target === drawer) closeDrawer();
        });

        // Toggle active state visual for Inputs inside labels
        document.querySelectorAll('.q-chip input').forEach(input => {
            input.addEventListener('change', function() {
                if(this.type === 'radio') {
                    document.querySelectorAll(`input[name="${this.name}"]`).forEach(el => el.parentElement.classList.remove('active'));
                    this.parentElement.classList.add('active');
                } else {
                    this.parentElement.classList.toggle('active', this.checked);
                }
            });
        });

        function toggleTheme() {
            const current = document.documentElement.getAttribute('data-theme');
            const next = current === 'light' ? 'dark' : 'light';
            document.documentElement.setAttribute('data-theme', next);
            localStorage.setItem('theme', next);
        }
    </script>
</body>
</html>