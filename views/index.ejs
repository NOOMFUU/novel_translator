<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ห้องสมุดนิยาย</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <header class="main-header">
        <div class="container header-content">
            <a href="/" class="logo">
                <i class="fa-solid fa-book"></i> 
                <span>Novel<span style="font-weight:400; color:var(--text-muted);">Translator</span></span>
            </a>
            
            <form action="/" method="GET" style="flex: 1; max-width: 400px; margin: 0 20px; position: relative;">
                <input type="text" name="q" placeholder="ค้นหาชื่อเรื่อง..." value="<%= locals.query || '' %>" style="padding-right: 40px;">
                <button type="submit" style="position: absolute; right: 10px; top: 50%; transform: translateY(-50%); background: none; border: none; color: var(--text-muted); cursor: pointer;">
                    <i class="fa-solid fa-magnifying-glass"></i>
                </button>
            </form>

            <div class="d-flex align-center gap-2">
                <button onclick="toggleTheme()" class="btn btn-outline btn-sm" title="เปลี่ยนโหมดสี"><i class="fa-solid fa-moon"></i></button>
                <% if (isAdmin) { %>
                    <a href="/logout" class="btn btn-sm btn-outline" style="color:var(--danger); border-color:var(--border);">Logout</a>
                <% } else { %>
                    <a href="/login" class="btn btn-sm btn-primary">Login</a>
                <% } %>
            </div>
        </div>
    </header>

    <div class="container" style="padding-top: 30px; padding-bottom: 40px;">
        
        <% if (isAdmin) { %>
        <div class="card">
            <h3 class="mb-4" style="font-size: 1.1rem;"><i class="fa-solid fa-plus-circle"></i> เพิ่มนิยายเรื่องใหม่</h3>
            <form action="/novels" method="POST" class="d-flex gap-2" style="flex-wrap: wrap;">
                <input type="text" name="title" placeholder="ชื่อนิยาย..." required style="flex: 1; min-width: 250px;">
                <input type="text" name="description" placeholder="เรื่องย่อ..." style="flex: 2; min-width: 250px;">
                <button type="submit" class="btn btn-primary" style="white-space: nowrap;">สร้างโปรเจกต์</button>
            </form>
        </div>
        <% } %>

        <div class="d-flex justify-between align-center mb-4">
            <h2 style="font-size: 1.5rem; color: var(--primary);">
                นิยายทั้งหมด <span class="text-muted" style="font-size: 0.6em; font-weight: 400;">(<%= novels.length %> เรื่อง)</span>
            </h2>
        </div>
        
        <% if (novels.length === 0) { %>
            <div class="card text-center" style="padding: 60px 20px; background: transparent; border: 2px dashed var(--border); box-shadow: none;">
                <i class="fa-regular fa-folder-open" style="font-size: 3em; color: var(--border); margin-bottom: 15px;"></i>
                <p class="text-muted">ยังไม่มีนิยายในคลัง</p>
            </div>
        <% } %>

        <div class="novel-grid">
            <% novels.forEach(novel => { %>
                <a href="/novel/<%= novel._id %>" class="novel-card">
                    <div class="novel-card-body">
                        <div class="novel-card-title"><%= novel.title %></div>
                        <div class="novel-card-desc"><%= novel.description || "ไม่มีคำอธิบาย" %></div>
                    </div>
                    <div class="novel-card-footer">
                        <span><i class="fa-regular fa-clock"></i> <%= new Date(novel.createdAt).toLocaleDateString('th-TH') %></span>
                        <span style="color: var(--accent); font-weight: 600;">อ่านเลย <i class="fa-solid fa-arrow-right"></i></span>
                    </div>
                </a>
            <% }) %>
        </div>
    </div>

    <script>
        document.documentElement.setAttribute('data-theme', localStorage.getItem('theme') || 'light');
        function toggleTheme() {
            const next = document.documentElement.getAttribute('data-theme') === 'light' ? 'dark' : 'light';
            document.documentElement.setAttribute('data-theme', next);
            localStorage.setItem('theme', next);
        }
    </script>
</body>
</html>