<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Novel Library</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .novel-grid { 
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); 
            gap: 24px; 
        }
        
        /* Mobile Responsive */
        @media (max-width: 600px) {
            .novel-grid { grid-template-columns: 1fr; } 
            .header-content { flex-direction: column; gap: 15px; text-align: center; }
            .logo { font-size: 1.5rem; margin-bottom: 5px; }
            .admin-bar { width: 100%; justify-content: center; }
        }
    </style>
</head>
<body>
    <header class="main-header">
        <div class="container header-content" style="display: flex; justify-content: space-between; align-items: center;">
            <div class="logo"><i class="fa-solid fa-book-open-reader"></i> Novel Translator AI</div>
            
            <div class="admin-bar" style="display: flex; gap: 10px; align-items: center;">
                <button onclick="toggleTheme()" class="btn btn-outline btn-sm" title="เปลี่ยนธีม"><i class="fa-solid fa-moon"></i></button>
                
                <% if (isAdmin) { %>
                    <span style="background:var(--success, green); color:white; padding:4px 10px; border-radius:20px; font-size:0.8em; font-weight:bold;">
                        <i class="fa-solid fa-check-circle"></i> Admin
                    </span>
                    <a href="/logout" class="btn btn-sm btn-outline" style="color: var(--danger, red); border-color: var(--danger, red);">
                        <i class="fa-solid fa-sign-out-alt"></i> Logout
                    </a>
                <% } else { %>
                    <a href="/login" class="btn btn-sm btn-outline">
                        <i class="fa-solid fa-key"></i> Login
                    </a>
                <% } %>
            </div>
        </div>
    </header>

    <div class="container">
        
        <% if (isAdmin) { %>
        <div class="card" style="border-left: 5px solid var(--primary);">
            <h3 class="mb-2"><i class="fa-solid fa-plus-circle"></i> เริ่มโปรเจกต์ใหม่</h3>
            <form action="/novels" method="POST" class="d-flex gap-2" style="flex-wrap: wrap;">
                <input type="text" name="title" placeholder="ชื่อนิยาย..." required style="flex: 1; min-width: 200px; padding: 10px; border-radius: 8px; border: 1px solid var(--border);">
                <input type="text" name="description" placeholder="เรื่องย่อสั้นๆ..." style="flex: 2; min-width: 200px; padding: 10px; border-radius: 8px; border: 1px solid var(--border);">
                <button type="submit" class="btn btn-primary" style="white-space: nowrap;">สร้างเลย</button>
            </form>
        </div>
        <% } %>

        <div class="d-flex align-center justify-between mb-4 mt-4">
            <h2><i class="fa-solid fa-layer-group"></i> นิยายในคลัง (<%= novels.length %>)</h2>
        </div>
        
        <% if (novels.length === 0) { %>
            <div style="text-align: center; padding: 60px; color: var(--text-muted);">
                <i class="fa-regular fa-folder-open" style="font-size: 4em; margin-bottom: 20px; opacity: 0.3;"></i>
                <p>ยังไม่มีนิยาย <%= isAdmin ? 'เริ่มต้นด้วยการเพิ่มเรื่องใหม่ด้านบน!' : 'รอแอดมินอัปเดตนิยายใหม่เร็วๆ นี้' %></p>
            </div>
        <% } %>

        <div class="novel-grid">
            <% novels.forEach(novel => { %>
                <a href="/novel/<%= novel._id %>" class="novel-card" style="text-decoration: none; color: inherit; display: block; border: 1px solid var(--border); border-radius: 12px; overflow: hidden; transition: transform 0.2s; background: var(--bg-card);">
                    <div class="novel-card-body" style="padding: 20px;">
                        <div class="novel-card-title" style="font-size: 1.2rem; font-weight: bold; margin-bottom: 10px;"><%= novel.title %></div>
                        <div class="novel-card-desc" style="color: var(--text-muted); font-size: 0.9rem; display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical; overflow: hidden;"><%= novel.description || "ไม่มีคำอธิบาย" %></div>
                    </div>
                    <div class="novel-card-footer" style="padding: 15px 20px; background: var(--bg-body); border-top: 1px solid var(--border); display: flex; justify-content: space-between; align-items: center; font-size: 0.85rem;">
                        <span style="color: var(--text-muted);"><i class="fa-regular fa-clock"></i> <%= new Date(novel.createdAt).toLocaleDateString('th-TH') %></span>
                        <span style="color: var(--primary); font-weight: 600;">อ่านเลย <i class="fa-solid fa-arrow-right"></i></span>
                    </div>
                </a>
            <% }) %>
        </div>
    </div>

    <script>
        document.documentElement.setAttribute('data-theme', localStorage.getItem('theme') || 'light');
        function toggleTheme() {
            const next = document.documentElement.getAttribute('data-theme') === 'light' ? 'dark' : 'light';
            document.documentElement.setAttribute('data-theme', next);
            localStorage.setItem('theme', next);
        }
    </script>
</body>
</html>